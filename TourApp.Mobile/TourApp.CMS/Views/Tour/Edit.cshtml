@model TourApp.CMS.Models.Tour

@{
    ViewData["Title"] = "Chỉnh sửa Tour";
}

<div class="page-breadcrumb">
    <i class="fas fa-suitcase"></i> Quản lý / Tour / <span class="text-primary">Chỉnh sửa</span>
</div>

<div class="page-title">✏️ Chỉnh sửa Tour</div>

<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header bg-white">
                <h5 class="mb-0">Thông tin Tour</h5>
            </div>
            <div class="card-body">
                <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
                    <input type="hidden" asp-for="Id" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" style="display:none"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold">Tên Tour <span class="text-danger">*</span></label>
                        <input asp-for="Name" class="form-control form-control-lg" placeholder="Ví dụ: Tour Hà Nội 3 ngày 2 đêm" required />
                    </div>

                    <div class="mb-3">
                        <label asp-for="Destination" class="form-label fw-bold">Điểm Đến <span class="text-danger">*</span></label>
                        <input asp-for="Destination" class="form-control" placeholder="Ví dụ: Hà Nội, Hạ Long" required />
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label fw-bold">Mô tả <span class="text-danger">*</span></label>
                        <textarea asp-for="Description" class="form-control" rows="6" placeholder="Nhập mô tả chi tiết về tour..." required></textarea>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Duration" class="form-label fw-bold">Thời gian (Ngày) <span class="text-danger">*</span></label>
                            <input asp-for="Duration" type="number" min="1" class="form-control" placeholder="Ví dụ: 3" required />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="MaxParticipants" class="form-label fw-bold">Số Khách Tối Đa <span class="text-danger">*</span></label>
                            <input asp-for="MaxParticipants" type="number" min="1" class="form-control" placeholder="Ví dụ: 30" required />
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Price" class="form-label fw-bold">Giá (VNĐ) <span class="text-danger">*</span></label>
                            <input asp-for="Price" type="number" step="0.01" min="0" class="form-control" placeholder="Ví dụ: 5000000" required />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="IsActive" class="form-label fw-bold">Trạng thái</label>
                            <div class="form-check form-switch">
                                <input asp-for="IsActive" type="checkbox" class="form-check-input" id="isActive" />
                                <label class="form-check-label" for="isActive">
                                    Hoạt động
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="mt-4 d-flex gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="fas fa-save"></i> Lưu Thay đổi
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                            <i class="fas fa-times"></i> Hủy
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card">
            <div class="card-header bg-white">
                <h5 class="mb-0">ℹ️ Thông tin</h5>
            </div>
            <div class="card-body">
                <p><strong>ID:</strong> <span class="badge bg-secondary">@Model.Id</span></p>
                <p><strong>Ngày tạo:</strong> @Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</p>
            </div>
        </div>
    </div>
</div>
